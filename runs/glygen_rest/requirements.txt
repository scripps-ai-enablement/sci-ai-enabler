# Pinned client versions for glyco_variants.py (reproducible runs).
#
# BioMCP client -> provides the `biomcp` CLI (`biomcp search variant`,
# `biomcp get variant ... clinvar predictions`): the variant_searcher /
# variant_getter surface used for the ClinVar + AlphaMissense join.
biomcp-python==0.8.25
# MCP SDK (optional GlyGen MCP backend; also a biomcp-python dependency).
mcp==1.9.4
# IEEE-2791 BioCompute Object schema validation.
jsonschema==4.22.0
# Only needed if you swap urllib for requests as the HTTP transport.
requests==2.32.3
