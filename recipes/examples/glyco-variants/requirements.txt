# Pinned environment for the LIVE run + BCO validation of glyco_variants.py.
#
# The offline replay itself is standard-library only and reads fixtures/ — it
# still *emits* the IEEE-2791 BCO with no deps. These packages are needed for:
#   - the live run (mcp, requests) — drives the GlyGen MCP server + UniProt;
#   - validating the emitted BCO against the bundled IEEE-2791 schema
#     (jsonschema, referencing). Without them, the script still writes the BCO
#     and reports validation as "skipped".
#
# BioMCP is NOT a pip dependency: the live run shells out to the `biomcp` CLI,
# installed out-of-band as the recipe prescribes:
#   uv tool install biomcp-cli        # provides `biomcp` (this run used 0.8.25)
mcp==1.28.1
requests==2.33.0
jsonschema==4.23.0
referencing==0.35.1
